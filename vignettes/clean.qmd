---
title: "edstr_clean() : nettoyage du texte"

vignette: >
  %\VignetteIndexEntry{edstr_clean() : nettoyage du texte}
  %\VignetteEngine{quarto::html}
  %\VignetteEncoding{UTF-8}
---

```{r}
library(edstr)
```

## Prérequis test

La fonction `edstr_clean()` dépend des paramètres définis avec `edstr_config()` : celle-ci doit alors être exécutée en amont.

```{r}
edstr_config(
  edstr_dirname = "demo/collect/data",
  edstr_filename = "avc",
  edstr_overwrite = FALSE
)
```

La fonction `edstr_clean` produit un dataframe portant le nom défini dans `edstr_config()` (argument `dest_filename`) suivi du suffixe `_clean`. Dans cet exemple, il sera donc nommé glue("{.config$file}_clean").

Il peut être produit de deux façons :

  - Nettoyage d'un dataframe chargé dans l'environnement
  - Chargement d'un dataframe déjà nettoyé

## Nettoyage d'un dataframe

```{r}
df_import <- edstr_import()
df_import
```

On peut ensuite procéder à son nettoyage avec `edstr_clean()`, en appliquant a minima une règle via l'argument `replace`.

Le dataframe nettoyé est automatiquement sauvegardé en .RData dans le répertoire défini avec `edstr_config()` (argument `dest_dir`).

```{r}
df_clean <- edstr_clean(
  data = df_import[1:100, ],
  replace = c("fracture" = "carotte")
)
df_clean
```
